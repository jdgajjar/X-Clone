# Production environment configuration for React frontend

# API base URL for production (replace with your actual Render.com backend URL)
VITE_API_URL=https://your-backend-app-name.onrender.com

# Environment configuration
NODE_ENV=production
VITE_NODE_ENV=production

# Build optimizations
GENERATE_SOURCEMAP=false